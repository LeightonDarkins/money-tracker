module.exports = {
  getSpinner: (wrapper) => wrapper.find('.account-list-spinner'),
  getPlaceholder: (wrapper) => wrapper.find('.placeholder'),
  getAccountListItems: (wrapper) => wrapper.find('.account-list li')
}
